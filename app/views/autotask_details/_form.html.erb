<%= form_for(@autotask_detail) do |f| %>
  <% if @autotask_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@autotask_detail.errors.count, "error") %> prohibited this autotask_detail from being saved:</h2>

      <ul>
      <% @autotask_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :autotask_id %><br>
    <%= f.number_field :autotask_id %>
  </div>
  <div class="field">
    <%= f.label :item_count %><br>
    <%= f.number_field :item_count %>
  </div>
  <div class="field">
    <%= f.label :time_interval %><br>
    <%= f.number_field :time_interval %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
