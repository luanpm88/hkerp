	<table class="table table-striped no-more-tables datatable-worksheet-expenses no-filter">
		<thead>
			<tr>
				<th style="width:1%" >
                    <div class="checkbox check-default">
						<input id="checkbox10" type="checkbox" value="1" class="checkall">
						<label for="checkbox10"></label>
					</div>
                </th>
				<th width="30%">Expenses Name</th>
				<th class="text-right">Price (VNĐ)</th>
				<th class="text-center">Type</th>
				<th class="text-center">Description</th>
				<th class="text-center">Created At</th>
				<th class="text-center">Status</th>	
				<th class="text-center">Creator</th>
				<th class="text-center">Actions</th>   
			</tr>
		</thead>
		<tbody>
		<!--  
		<% @worksheet_expenses.each do |expense| %>
		<tr>
			<td class="v-align-middle">
				<div class="checkbox check-default">
					<input id="checkbox11" type="checkbox" value="1">
					<label for="checkbox11"></label>
				</div>
			</td>
			<td class="v-align-middle"><%= expense.name %></td>
			<td class="v-align-middle"><%= expense.price %></td>
			<td class="v-align-middle">
				<% if expense.type_name == 'all' %>
						All
				<% elsif expense.type_name == 'per_km' %>
						Per_Kilometer
				<% end %>
			</td>
			<td class="v-align-middle"><%= expense.description %></td>
			<td class="v-align-middle"><%= expense.created_at %></td>
			<td class="v-align-middle"><%= expense.creator.short_name %></td>
			<td>
			<%= link_to '<i class="icon-zoom-in"></i>'.html_safe, expense, :class => "btn btn-success btn-xs btn-mini tmp_hide" %>
			<%= link_to '<i class="icon-paste"></i>'.html_safe, edit_worksheet_expense_path(id: expense.id, tab_page: 1),psrc: worksheet_expenses_url(tab_page: 1), title: "Edit", :class => "tab_page btn btn-info btn-xs btn-mini" %>														    
			<%= link_to 'X', {action: "destroy", id: expense.id, tab_page: 1}, title: "Delete", method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger btn-xs btn-mini" %>
			</td>
		</tr>
		<% end %>
		-->
	
		  </tbody>
	</table>