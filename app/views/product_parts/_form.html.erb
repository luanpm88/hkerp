<%= form_for(@product_part) do |f| %>
  <% if @product_part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_part.errors.count, "error") %> prohibited this product_part from being saved:</h2>

      <ul>
      <% @product_part.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :part_id %><br>
    <%= f.number_field :part_id %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
