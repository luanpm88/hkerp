<%= form_for(@autotask) do |f| %>
  <% if @autotask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@autotask.errors.count, "error") %> prohibited this autotask from being saved:</h2>

      <ul>
      <% @autotask.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :item_count %><br>
    <%= f.number_field :item_count %>
  </div>
  <div class="field">
    <%= f.label :time_interval %><br>
    <%= f.number_field :time_interval %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
