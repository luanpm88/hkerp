                <ul class="breadcrumb">
                    <li>
                        <p>YOU ARE HERE</p>
                    </li> 
                    <li>Accounting</li>
                </ul>
                <div class="page-title">
                        <i class="icon-archive"></i>
                    	<h3>Pay Commission</h3>
                </div>
                <div class="row">
                    <div class="col-md-12">			
                        <div class="grid simple ">
                            <div class="grid-title no-border">
                                <h4>Order:  <span class="semi-bold"><%= @order.quotation_code %></span></h4>
                                <h4><%= @order.is_purchase ? "Supplier" : "Customer" %>:
                                    <span class="semi-bold">
                                      <%= @order.is_purchase ? @order.supplier.name : @order.customer.name %>
                                    </span>
                                </h4>
                                <h4><%= @order.is_purchase ? "Purchaser" : "Salesperson" %>:
                                    <span class="semi-bold">
                                      <%= @order.is_purchase ? @order.purchaser.name : @order.salesperson.name %>
                                    </span>
                                </h4>
                                <br /><br />
                                <h4>Commission Details:</h4>
                                <div class="tools">
				    
				    <a href="javascript:;" class="collapse"></a>
                                    <a href="#grid-config" data-toggle="modal" class="config"></a>
				    <a href="javascript:;" class="reload"></a>
				    <a href="javascript:;" class="remove"></a>
                                    
                                </div>
                            </div>
                            <div class="grid-body no-border">
							
							<%= render "commission_form" %>
							
                            </div>
                        </div>
			
                    </div>
		    
                </div>
                
